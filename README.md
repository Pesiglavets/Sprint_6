# Sprint_6
1. Основа для написания автотестов — фреймворк pytest.
2. Установить зависимости из requirements.txt или pip install selenium pytest webdriver-manager allure
3. Команда для запуска — pytest tests/ --alluredir=allure_results
4. Посмотреть отчет: allure serve allure_results